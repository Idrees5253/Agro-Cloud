# Copy to .env for local dev if your backend isn't on localhost:5000,
# or set this as an Environment Variable in Vercel for production.
# NOTE: no trailing slash, no /api at the end — just the base server URL.
VITE_API_BASE=http://localhost:5000
